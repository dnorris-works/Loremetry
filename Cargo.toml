[workspace]
resolver = "2"
members = ["crates/core", "crates/web", "crates/worker"]
default-members = ["crates/web"]
exclude = ["src-tauri"]

# Web app only. Desktop Tauri app lives in Loremetry-Desktop (separate repo).
